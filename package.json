{
  "name": "missed-call-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "schema:remote": "wrangler d1 execute missedcall_ai_leads --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}
